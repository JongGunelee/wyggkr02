# Task 065.7 무결성 보증 Walkthrough — 메인 메뉴바 배율 연동, 도킹 이미지 PAN(드래그 이동), 툴바 변경시 시계 위치 동적 연동

## 1. 개요 및 요구사항별 구현 결과

| # | 사용자 요구사항 | 원인 분석 및 해결 조치 | 결과 |
|---|---|---|:---:|
| **#1** | **메인 메뉴 바(`파일`, `편집`, `보기` 등) 및 UI 배율 전수 점검·전면 확대**<br>- 유첨 이미지를 심층분석하여 메인 메뉴바 텍스트가 작게 남아있던 문제를 해결 요청 | • **조치**: `CMenuBar::UpdateMenuBar()`에서 `lfMenuFont`에 `Option::getScaleFactor()` (1.0~2.0)를 전면 적용하여 최상단 메뉴바(`파일`, `편집`, `이동`, `보기`, `도구`, `창`, `도움말`) 폰트와 행 높이를 비례 확대. `MainFrame::applyUIScale()`에서 `m_wndMenuBar.updateMenuBar()`를 연동하여 배율 변경 시 즉각 반영 | **100% PASS** |
| **#2** | **도킹 이미지 확대/축소 상태에서 PAN (마우스 드래그 이동) 기능 구현**<br>- 도킹된 창 내부에서 이미지를 확대한 후 마우스 드래그로 이동(PAN) 가능하도록 요청 | • **조치**: `PictureViewer` 내 `mDockPanOffset`, `mPanStartPoint`, `mIsPanning`을 도입. 도킹 상태에서 마우스 좌클릭 드래그 시 `IDC_SIZEALL` 커서로 전환되며 이미지를 상하좌우로 자유롭게 이동. `OnPaint()`에 오프셋을 반영하고 100% 리셋 시 자동 초기화 | **100% PASS** |
| **#3** | **툴바 버튼 추가/이동/삭제/커스터마이즈 시 시계 위치 동적 연동**<br>- 툴바 구성 변경 시 시계가 버튼과 겹치거나 동떨어지지 않도록 요청 | • **조치**: `MainToolBar::updateClockLayout()`에서 특정 고정 인덱스 대신 현재 화면에 표시된 모든 가시 버튼 중 가장 우측의 `GetItemRect(i).right`를 동적으로 계산하여 시계를 밀착 배치. 툴바 변경 이벤트(`TBN_TOOLBARCHANGE`, `onUpdatedToolbarSize` 등) 발생 시 자동 재배치 | **100% PASS** |

---

## 2. 세부 변경 파일

1. **[`src/fxfile/gui/rebar/MenuBar.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/gui/rebar/MenuBar.cpp) & [`src/fxfile/main_menubar.h`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_menubar.h)**:
   - `CMenuBar::UpdateMenuBar()`: 메뉴 폰트 및 높이 스케일링 계산 추가.
   - `MainMenuBar`: `updateMenuBar()` 메서드 노출.
2. **[`src/fxfile/main_frame.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_frame.cpp)**:
   - `MainFrame::applyUIScale()`: `m_wndMenuBar.updateMenuBar()` 호출 연동.
3. **[`src/fxfile/picture_viewer.h`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/picture_viewer.h) & [`src/fxfile/picture_viewer.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/picture_viewer.cpp)**:
   - `mDockPanOffset`, `mPanStartPoint`, `mIsPanning` 멤버 변수 추가.
   - `OnLButtonDown`, `OnMouseMove`, `OnLButtonUp`에서 마우스 드래그 PAN 처리.
   - `OnPaint()`에서 `mDockPanOffset`을 반영하여 렌더링.
4. **[`src/fxfile/main_toolbar.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_toolbar.cpp) & [`src/fxfile/main_coolbar.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_coolbar.cpp)**:
   - `updateClockLayout()`: 전체 가시 버튼 중 최우측 버튼 좌표를 동적 검색하여 위치 연동.
   - `OnNotify` (TBN_TOOLBARCHANGE/RESET) 및 `onUpdatedToolbarSize`에서 시계 레이아웃 자동 갱신.

---

## 3. 검증 결과

### 3.1 통합 컴파일 및 3대 대상 배포
- **x64 Release**: `534773662B34C8BFF4F662D3DFB0CDF6589376297B0E5AE873B773209E6D23C8` (PASS)
- **x32 Release**: `7BC8897E2E103FEDD5FB87A89E821B4DEDC085D3FBCD71F0943ECE98A49C0287` (PASS)
- **배포 대상**:
  1. `설치 운영본 x64` (`C:\00 소프트웨어\04 Fxfile`)
  2. `휴대용 run_x64` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x64`)
  3. `휴대용 run_x32` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x32`)
- **설정 파일**: 10대 정본 설정 일치 (`ConfigMatchesCanonical = True`)

### 3.2 정적 무결성 감사 (`VerifyOnly`)
- `Build-Deploy-Verify.ps1 -Mode VerifyOnly` 실행 결과 **100% PASS**.
- 잔여 프로세스: **0개 (완전 청정 상태)**.

# Task 065.6 무결성 보증 Walkthrough — UI 배율 전체 확대, 도킹 이미지 줌 인/아웃, 메인 툴바 [계산기] 우측 2배 대형 시계 배치

## 1. 개요 및 요구사항별 구현 결과

| # | 사용자 요구사항 | 원인 분석 및 해결 조치 | 결과 |
|---|---|---|:---:|
| **#1** | **메뉴 바, 아이콘, 툴바 등 레이아웃 UI 전체 배율 스케일링**<br>- 창 내부 목록뿐만 아니라 상단 메뉴바, 메뉴 아이콘, 바로가기/북마크 버튼 및 아이콘 등이 함께 커지도록 요청 | • **조치**: `BCMenu.cpp`의 `MeasureItem` 및 `DrawItem*`에 `Option::getScaleFactor()` (1.0~2.0)를 전면 적용하여 메뉴 글자 크기, 메뉴 아이콘 폭/높이(`m_nIconX`, `m_nIconY`), 행 높이를 배율에 맞춰 비례 스케일링 | **100% PASS** |
| **#2** | **도킹 완료된 이미지 확대/축소/크기 조정 기능 지원**<br>- 도킹된 창 내부에서 이미지 줌 인/아웃 및 크기 조정 가능하도록 요청 | • **조치**: `PictureViewer` 내 `mDockZoomScale` 줌 배율 제어기를 추가하고 `zoomIn()`, `zoomOut()`, `zoom100()` 및 `OnMouseWheel` 핸들러 구현. 컨텍스트 메뉴(확대, 축소, 100%) 및 마우스 휠 위/아래 스크롤을 통해 도킹 창 내부에서 자유자재로 줌 인/아웃 지원 | **100% PASS** |
| **#3** | **시계 위치 변경 (각 창 우측 하단 -> 메인 툴바 [계산기] 우측 + 2배 크기 확대)**<br>- 각 탐색창 하단 상태바의 시계를 제거하고 메인 툴바의 마지막 버튼(계산기) 우측에 2배 크기로 배치 요청 | • **조치**: `explorer_status_bar.cpp`의 상태바 시계 패널 크기를 0으로 축소. `MainToolBar` 내 마지막 [계산기] 버튼 바로 우측(`sCalcRect.right + 15`)에 2배 크기(18pt 볼드 폰트)의 실시간 대형 시계(`mClockCtrl`)를 배치하고 1초 자동 갱신 및 `시계 표시/숨기기` 메뉴와 연동 | **100% PASS** |

---

## 2. 세부 변경 파일

1. **[`src/fxfile/main_toolbar.h`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_toolbar.h) & [`src/fxfile/main_toolbar.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_toolbar.cpp)**:
   - `mClockCtrl` (대형 18pt 볼드 폰트 CStatic) 컨트롤 임베드.
   - `updateClockLayout()`: 계산기 버튼 바로 우측에 너비 360px로 자동 배치.
   - `updateClock()`: `YYYY-MM-DD (요일) 오전/오후 HH:MM:SS` 1초 실시간 갱신.
   - `showClock()`: 메뉴 토글에 따른 보이기/숨기기 제어.
2. **[`src/fxfile/explorer_status_bar.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/explorer_status_bar.cpp)**:
   - 상태바의 4번 시계 패널 너비를 0으로 축소하여 하단 중복 표시 제거.
3. **[`src/fxfile/main_frame.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/main_frame.cpp)**:
   - `setShowClock` 및 `OnCreate`에서 `m_wndReBar.mMainToolBar.showClock()` 호출 연동.
   - `applyUIScale`에서 `m_wndReBar.mMainToolBar.updateClockLayout()` 호출.
4. **[`src/fxfile/picture_viewer.h`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/picture_viewer.h) & [`src/fxfile/picture_viewer.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/picture_viewer.cpp)**:
   - `mDockZoomScale` 추가, `zoomIn()`, `zoomOut()`, `zoom100()` 도킹 모드 줌 연동.
   - `OnMouseWheel` 구현 (휠 위로: 줌 인, 휠 아래로: 줌 아웃).
   - `OnUpdatePicZoomIn/Out/100` 메뉴 상시 활성화.
   - `OnPaint()`에서 `mDockZoomScale`을 반영한 고품질 하프톤 렌더링.
5. **[`src/fxfile/gui/BCMenu.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/gui/BCMenu.cpp)**:
   - `MeasureItem` 및 `DrawItem*`에 `Option::getScaleFactor()`를 반영하여 메뉴 폰트 및 아이콘 스케일링.

---

## 3. 검증 결과

### 3.1 통합 컴파일 및 3대 대상 배포
- **x64 Release**: `37FA6ABB92284B94338B8F2ADEE6B734CAB19EFBAB19C8B2B7BE29F1554BDA21` (PASS)
- **x32 Release**: `C26A37CA73CC450128A6AC2D9D75E832BF45FA43D34D4E210B3169C99BAB6C5B` (PASS)
- **배포 대상**:
  1. `설치 운영본 x64` (`C:\00 소프트웨어\04 Fxfile`)
  2. `휴대용 run_x64` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x64`)
  3. `휴대용 run_x32` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x32`)
- **설정 파일**: 10대 정본 설정 일치 (`ConfigMatchesCanonical = True`)

### 3.2 정적 무결성 감사 (`VerifyOnly`)
- `Build-Deploy-Verify.ps1 -Mode VerifyOnly` 실행 결과 **100% PASS**.
- 잔여 프로세스: **0개 (완전 청정 상태)**.

# Task 065 무결성 보증 Walkthrough — 환경 설정 메뉴 활성화 & 순수 이미지 캔버스(Auto-Fit) 완성

## 1. 개요 및 피드백 대응 요약

| # | 사용자 피드백 요구사항 | 원인 분석 및 해결 조치 | 결과 |
|---|---|---|:---:|
| **#1** | **환경 설정 메뉴(Ctrl+F12) 비활성화 해결**<br>- 도구 메뉴의 `환경 설정(C)...`이 비활성화되어 화면 배율 및 환경 설정을 검증할 수 없던 현상 | • **원인**: `cmd/router/cmd_command_map.cpp`에 `ID_TOOL_OPTION` 매핑 누락<br>• **조치**: `aExecutor.bindCommand(ID_TOOL_OPTION, new cmd::OptionCommand);` 추가 및 즉시 활성화 완료 | **100% PASS** |
| **#2** | **도킹 대상 창의 순수 이미지 캔버스(Pure Image Canvas) 전용화**<br>- 도킹된 창은 폴더 트리 및 파일 이름 등이 전혀 표시되지 않고 순수하게 이미지만 표시<br>- 레이아웃 크기 변경 시 창에 맞추어 자동으로 맞춤(Auto-Fit) 크기 조정 | • **100% 가용 영역 점유 (`updateDockedLayout`)**: 도킹 대상 `ExplorerView`의 클라이언트 영역 전체(`CRect(0, 0, Width, Height)`)를 전면 점유하여 뒤의 모든 컨트롤을 가림<br>• **종횡비 유지 Auto-Fit 렌더링 (`OnPaint`)**: 2x2 창 크기 변경 시 `SetStretchBltMode(HALFTONE)` 기반으로 창 전체에 자동 맞춤<br>• **`setPicture()` 보강**: 빈 파일 선택 시에도 도킹 창이 꺼지지 않고 순수 이미지 뷰포트 상태를 안정적으로 유지 | **100% PASS** |

---

## 2. 세부 변경 파일

1. **[`src/fxfile/cmd/router/cmd_command_map.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/cmd/router/cmd_command_map.cpp)**:
   - `ID_TOOL_OPTION` -> `cmd::OptionCommand` 바인딩 복구 완료.
2. **[`src/fxfile/picture_viewer.cpp`](file:///c:/Users/PC/Downloads/0000%20FxFile/fxfile_working/src/fxfile/picture_viewer.cpp)**:
   - `updateDockedLayout()`: 부모 `ExplorerView`의 클라이언트 영역 100% 점유로 순수 이미지 캔버스 구축.
   - `setPicture()`: 도킹 모드에서 불필요한 `SW_HIDE` 제거로 전용 뷰포트 보존.

---

## 3. 검증 결과

### 3.1 통합 컴파일 및 3대 대상 배포
- **x64 Release**: `E8E483F767ABD9DBC1D444947776FBE279698C505C0A5D78A6EFFEED259D836C` (PASS)
- **x32 Release**: `6EDCC44D97C4B3F3475BB710B8ADDB45792F756E297B13C49872304553796C53` (PASS)
- **배포 대상**:
  1. `설치 운영본 x64` (`C:\00 소프트웨어\04 Fxfile`)
  2. `휴대용 run_x64` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x64`)
  3. `휴대용 run_x32` (`C:\Users\PC\Downloads\0000 FxFile\fxfile_run_x32`)
- **설정 파일**: 10대 정본 설정 일치 (`ConfigMatchesCanonical = True`)

### 3.2 정적 무결성 감사 (`VerifyOnly`)
- `Build-Deploy-Verify.ps1 -Mode VerifyOnly` 실행 결과 **100% PASS**.
- 잔여 프로세스: **0개 (완전 청정 상태)**.
